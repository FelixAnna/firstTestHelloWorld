package com.xiangyong;

//import com.xiangyong.entity.ContactInfo;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
//import org.springframework.web.client.RestTemplate;

import static org.junit.Assert.assertEquals;

@RunWith(SpringRunner.class)
@SpringBootTest
public class FirstTestHelloWorldApplicationTests {

	@Test
	public void contextLoads() {
		//RestTemplate template =new RestTemplate();
		//ContactInfo contactInfo = template.getForObject("http://localhost:8080/contact/{id}", ContactInfo.class, 1);
		//assertEquals(con)
	}
}
